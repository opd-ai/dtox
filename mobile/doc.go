//go:build !android

// Package mobile is the Android entry point for the Tox Messenger application.
// On non-Android platforms this file is a placeholder so that tools like
// `go list ./...` and `go test ./...` do not fail with
// "build constraints exclude all Go files".
package mobile
